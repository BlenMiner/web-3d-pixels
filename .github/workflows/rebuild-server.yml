name: rebuild-server
on: 
  push:
    branches: ['main']
    paths: ['3d-pixels-server/**']
  workflow_dispatch:
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: 52.90.117.251
        username: ec2-user
        key: ${{ secrets.EC2 }}
        port: 22
        script: |
          [ -d "web-3d-pixels" ] || git clone https://github.com/BlenMiner/web-3d-pixels.git
          cd "web-3d-pixels/3d-pixels-server"
          git reset --hard @{u}
          git clean -df
          git pull
          killall -9 3d-pixels-server
          dotnet build --property:Configuration=Release
          ./bin/Release/net6.0/3d-pixels-server & disown